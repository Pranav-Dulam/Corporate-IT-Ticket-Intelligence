# Core data stack
pandas>=2.2,<3.0
numpy>=1.26,<3.0

# Machine learning (Phases 6-7)
scikit-learn>=1.5,<2.0
joblib>=1.4,<2.0

# Data acquisition
requests>=2.32,<3.0

# Configuration / secrets (Phase 9 onward)
python-dotenv>=1.0,<2.0

# Testing (Phase 11)
pytest>=8.0,<9.0

# Notebooks (optional, for exploratory analysis in Phase 5)
jupyterlab>=4.2,<5.0

# --- Added later, when the phase actually needs them ---
# Phase 8 (semantic retrieval): sentence-transformers  -> pulls in torch (~2 GB)
# Phase 9 (LLM layer):          openai  OR  anthropic
